{
  "name": "@allgo/shared",
  "version": "0.1.0",
  "private": true,
  "files": [
    "types",
    "constants"
  ],
  "types": "./types/index.ts",
  "license": "UNLICENSED",
  "scripts": {
    "build": "echo \"noop for shared types\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^4.1.2"
  }
}
